package com.intentlock.app.data

object DataSeeder {
    // DataSeeder is now deprecated in favor of User-Defined Schedules.
    // We keep it empty to prevent overriding user settings.
    suspend fun seed(db: AppDatabase) {}
}
